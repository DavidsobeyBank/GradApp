@model IEnumerable<GradApp.Models.ProjectWishList>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Index</h3>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Moivation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Graduate.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Project.ProjectName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Moivation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Graduate.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Project.ProjectName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WishListID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WishListID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WishListID })
        </td>
    </tr>
}

</table>
